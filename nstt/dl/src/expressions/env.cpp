#include "env.hpp"
    //
    // void Env::record(const std::string& id, Expression* e) { lookupTable[id] = e; }
    // Expression* Env::from_env(const std::string& id) {
    //     if (!lookupTable.count(id))
    //         throw std::runtime_error("no bounded expression for such id");
    //     return lookupTable[id];
    // }
